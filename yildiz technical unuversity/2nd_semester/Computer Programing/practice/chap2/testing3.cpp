#include <iostream>
#include <iomanip>
using namespace std;
int main()
{
    int hours;
    double grossPay, rate;
    cout << "enter the hourle rate of pay: \n";
    cin >> rate;
    cout <<" enter the num hours worked\n"
         <<"rounded to a whole num:";
    cin >> hours;
    cout << fixed << showpoint << setprecision(2)
    if (hours > 40)
        grossPay = rate * 40;
        cout << fixed << showpoint << setprecision(2);
}